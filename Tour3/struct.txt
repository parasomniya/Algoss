# Task 1

1. Получаем на вход 4 байта = Q - кол во чисел в массиве
2. for (int i = 0; i < Q; i++) - обрабатываем эти 
числа и записывает их в Хэш таблицу 

Хэш таблица -------
Надо создать "словарь" с значениями и ключами
Потом создаем хэш тэйбл с указателями на массив узлов
Потом надо функцию которая получает индекс в ефблице
Функция которая обнуляет все индексы в таблице
Надо функцию которая добавляет элемент в хэш табле 
 и проверяет есть ли уже такой элемент в табле
 если нет то добавляем элемент в следующий элемент